# MoshClientCore

- Clean-room implementation required; do not copy GPL mosh client code.
- Protocol behavior only; platform/UI integration lives elsewhere.
- Do not log secrets (session keys, private keys, passwords).
